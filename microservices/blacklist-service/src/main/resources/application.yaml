spring:
  application:
    name: blacklist-service

  # DATABASE (PostgreSQL)
  datasource:
    url: jdbc:postgresql://localhost:15432/blacklist_db
    username: sentinel
    password: sentinel123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
